Your goal is to update any vulnerable denpendecies

Do the following:

1. Run `npm audit` to find vulnerable dependencies installed packages in this project
2. Run `npm audit fix` to apply update
3. Run tests and verify the updates didn't break anything

